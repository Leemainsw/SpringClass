# **🐧 Spring 6강**

## path와 Query String의 구분
* path의 시작은 (/) 부터
* query string의 시작은 (?) 부터
* fragment의 시작은 (#) 부터


```java
@RequestMapping("/request_param3")
// Map<String, String>
public String requestParamTest3(@RequestParam Map<String, String> params) {
    String result = "";
    for(String key : params.keySet()) {
    result += (key + "/" + params.get(key) + " ");
    }
    return result;
}
```

@RequestParam Map<String, String> params

Q) 어떤식으로 동작할까?<BR>
A) 키는 맵의 키로, 값은 맵의 값으로 들어감


```java
	// @PathVariable을 이용하여 URL Path에 포함된 변수 가져오기 가능
	@GetMapping("/path_variable/{abc}/{def}")
	@ResponseBody 
	// 조건 1. 변수 이름과 Mapping에 들어가는 이름이 같아야 함 (abc)
	// 조건 2. @PathVariable 어노테이션에 전달된 값이 같아야 함 (def)
	public String pathVariableTest(@PathVariable Integer abc, @PathVariable("def") String asdf) {
		return abc + "," + asdf;
	}
```

/{abc} -> abc라는 이름으로 변수 받기
/{def} -> def라는 이름으로 변수 받기
 @PathVariable("def")
 => 어노테이션에 들어갈 문자열이 중괄호({def})안의 이름과 같아야 함

@RequestParam @PathVariable의 차이점
- PathVariable은 주로 RESTful API를 구성할 때 유용하게 사용됨 (물론 Query String도!)

```
path 변수 이용 사례)
api.github.com/users/{name}
api.github.com/users/hello
api.github.com/gists/{id}
api.github.com/gists/f59f1b5ce5e5015cdcf1c27ff1224684
파라미터 변수 이용 사례)
.../v1/locations/search?access_token=ACCESS_TOKEN&lat=40.7127&lng=74.0059
www.googleapis.com/youtube/v3/channels?part=contentDetails&mine=true
둘 다 동시에 쓸 수도 있음
www.hello.com/api/users/{name}/repo와 쿼리 스트링 조합
www.hello.com/api/users/hello/repo?type=public
```