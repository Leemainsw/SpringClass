# **🐧 Spring 5강**

모델 => 데이터를 잠는 용기
* th:text는 실제 html에서 사용하지 않는다.
* 템플릿 엔진이 사용하는 속성 (전처리 진행용)

```java
//sring 코드
@GetMapping("student")
public String student(Model m) {
  m.addAttribute("title", "Student page");
  m.addAttribute("student", new Student(1, "철수", "chulsoo@naver.com", false));

  return "student";
}
```

```html
<!-- html 코드 -->
<title th:text="${title}"></title>

<h1 th:text="${student.id}"></h1>
<h1 th:text="${student.name}"></h1>
<h1 th:text="${student.email}"></h1>
<h1 th:text="${student.genius}"></h1>
<!-- = -->
<h1 th:text="${student.getEmail()}"></h1>
```

```html
<h1 th:text="${student.id}"></h1> 
```
=> 마치 필드에 접근하는 것처럼 보이지만 실제로는 geter메소드 호출 함

**이렇게 임의의 객체를 전달하여 필드 접근 하듯이(실제로는 getter메소드 호출) 정보 보여주기 가능 (단, 클래스는 자바 빈(Bean) 클래스여야 함**

### Query String

* 물음표(?) 뒷 부분이 query string
* 정보의 구분 (&)
* 등호(=)를 기준으로 왼쪽이 키 오른쪽이 값 (왼키오값)

```java
@RequestMapping("/request_param")
@ResponseBody 
//return 할 때 템플릿이 아닌 그냥 값을 바로 찍고 싶어서 
public String requestParamTest(@RequestParam(value="name")String name) {
  return name;
}
```

**/request_param?name=sora** = **sora**

```java
// 파라미터값이 필수적으로 필요
public String requestParamTest(@RequestParam(value="name") String name)

// 파라미터값 필요 없음
public String requestParamTest(@RequestParam(value="name", required=false) String name)

// 파라미터값이 없으면 적용할 디폴트 값 설정
public String requestParamTest(@RequestParam(value="name", defaultValue="World") String
name)

// 이렇게 써도 파라미터 이름과 변수 이름이 일치하므로 값 전달 가능 (비권장)
public String requestParamTest(String name)

```